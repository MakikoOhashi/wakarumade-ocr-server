services:
  - type: web
    name: wakarumade-ocr-server
    # Dockerfileを使用してTesseractをインストール
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3333
